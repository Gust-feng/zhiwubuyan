# 知无不言 Windows 安装器

这个目录存放知无不言自己的安装器入口。它承载三段用户流程：选择安装位置、显示安装进度、完成后启动知无不言。

安装器外壳与应用包分开：外壳只负责安装体验和安装生命周期，应用包由 electron-builder 生成并由外壳启动。安装器不读取开放平台凭证，也不清理用户数据。

## 当前状态

界面原型已经就位，下一步接入 Windows 原生安装桥接后，使用以下命令生成发布包：

```powershell
pnpm build:installer-shell
```
